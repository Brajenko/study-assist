import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'DeepHack-AwareTeam AI Agent UI',
  webDir: 'dist'
};

export default config;
